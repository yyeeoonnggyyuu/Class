객체, 함수, 배열,

함수를 매개변수로 넣었다 하면 콜백함수


무언가를 편하게 다룰 수 있게 해주는게 메소드

콜백 함수를 활용하느 함수 : forEach()
forEach() 메소드는 배열이 갖고 잇는 함수(메소드)로써 단순하게 배열 내부의 요소를 사용해서 콜백 함수를 호출
function(value, index, array){}

변수선언 할 때 val 보다는 let이 더 좋다
let 이 전체 코드에 영향이 없어서 



        

        엄격모드
        여러 자바스크립트 코드를 보면 블록의 가장 위쪽에 'use strict'라는 문자열
        이는 엄격모드 (strict mode) 기능르로 자바스크립트는 이러한 문자열을 읽어들인 순간부터 코드를엄격하게 검사

        'use strict'
        문장
        문장

        'use strict' // 없이 하면 data를 let으로 변수를 선언해주만 use strict를 하면 출력안됨
        data=10
        console.log(data)


        익명함수의 사용 : 익명함수는 순차적인 코드 실행에서 코드가 해당 줄을 읽을 때 생성됨


        선언적 함수의 사용: 선언적 함수는 순차적인 코드 실행이 일어나기 전에 생성됨

        선언적 함수와 익명함수의 조합
        선언적 함수는 먼저 생성되고, 이후에 순차적인 코드 진행을 시작하면서 익명 함수를 생성.
        따라서 다음과같은 코드를 작성하면 순서와 관계없이 ""

        -과거 자바스크립트는 var이라는 키워드를 사용해서 변수를 선언
            1. var 키워드는 이전 코드처럼 덮어쓰는 문제가 발생
            2. 현대의 자바스크립트는 let 키워드와 const키워드를 사용해서 변수와 상수를 선언
            3. 이러한 키워드들은 위험을 원천적으로 차단하기 위홰서 오류를 발생
